{
  "name": "drone-report-server",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "tsc -p . && mkdir -p dist/templates && cp -r src/templates/* dist/templates/",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@drone-report/shared": "file:../shared",
    "dotenv": "^17.2.1",
    "express": "^4.19.2",
    "zod": "^4.1.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "typescript": "^5.5.4"
  }
}
