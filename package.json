{
  "name": "dronereport",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "client",
    "functions"
  ],
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build"
  }
}
