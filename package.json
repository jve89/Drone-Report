{
  "name": "dronereport",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "engines": { "node": "20.x" },
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "echo \"skip root build\"",
    "heroku-postbuild": "npm --prefix server ci --include=dev && npm --prefix server run build && npm --prefix client ci --include=dev && npm --prefix client run build"
  }
}
