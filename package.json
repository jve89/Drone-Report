{
  "name": "dronereport",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": ["client","server"],
  "engines": { "node": "20.x" },
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "heroku-postbuild": "npm --prefix server ci --include=dev && npm --prefix server run build && npm --prefix client ci && npm --prefix client run build && rm -rf server/public && mkdir -p server/public && cp -r client/dist/* server/public/"
  }
}
