{
  "name": "dronereport",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "client",
    "functions"
  ],
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "stripe": "^16.12.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.29"
  }
}
