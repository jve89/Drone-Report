{
  "name": "dronereport",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "engines": { "node": ">=20" },
  "workspaces": ["client", "server", "shared"],
  "scripts": {
    "dev": "npm -w client run dev",
    "build": "npm -w client run build && npm -w server run build",
    "preview": "npm -w client run preview",
    "build:server": "npm -w server run build",
    "heroku-postbuild": "npm -w server run build && npm -w client run build"
  }
}
